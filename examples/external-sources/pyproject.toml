[build-system]
requires = ["conan-py-build"]
build-backend = "conan_py_build.build"

[project]
name = "hello-bindings"
version = "0.1.0"
description = "Python bindings for libhello (C++ code fetched from GitHub in source())"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.8"
